name: Run TImer_docker
on: push

jobs:
  compile:
    name: Compile assets
    runs-on: ubuntu-latest
    steps:
      - name: Install Docker
        uses: papodaca/install-docker-action@v1

          
          
                
          
          
